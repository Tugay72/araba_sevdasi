import 'package:flutter/material.dart';

class Yolculuklar extends StatelessWidget {
  const Yolculuklar({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Geçmiş Yolculuklar"),
      ),
    );
  }
}
